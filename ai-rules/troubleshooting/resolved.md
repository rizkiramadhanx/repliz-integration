# Resolved Issues Archive

> **Status:** DATA FILE — Arsip issue yang sudah di-resolve dan stabil.
> **Purpose:** Referensi historis. Jangan hapus — bermanfaat jika masalah serupa muncul lagi.

---

## IMMUTABLE -- AI TIDAK BOLEH MENGUBAH FILE INI. Baca template ini, lalu BUAT file BARU di folder output (dev-docs/, planning/, dll) -- JANGAN ubah template ini.

**What to CREATE in output folder:** Pindahkan issue dari `README.md` ke sini setelah fix stabil >1 bulan. Format ringkas — hanya yang penting untuk referensi masa depan.

---

## Dependency Conflicts (Resolved)

| Date | Packages | Conflict | Resolution |
|------|----------|----------|-----------|
| `{YYYY-MM-DD}` | `{pkg-a vs pkg-b}` | `{konflik}` | `{solusi}` |

---

## Performance Issues (Resolved)

| Date | Area | Issue | Solution |
|------|------|-------|----------|
| `{YYYY-MM-DD}` | `{modul/query}` | `{n+1 problem / slow query}` | `{solusi}` |

---

## General Issues (Resolved)

| Date | Symptom | Root Cause | Solution |
|------|---------|-----------|----------|
| `{YYYY-MM-DD}` | `{gejala}` | `{penyebab}` | `{solusi}` |
